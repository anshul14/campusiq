{
  "deployment": {
    "name": "your-institution",
    "region": "aws-region",
    "environment": "dev | stage | prod "
  },
  "idp": {
    "type": "ENTRA_ID",
    "comment": "Options: ENTRA_ID | GOOGLE | SAML"
  },
  "cors": {
    "allowed_origins": "https://YOUR_NEXTJS_DOMAIN",
    "comment": "API Gateway CORS allowed origin for your frontend. Falls back to http://localhost:3000 if omitted."
  }
}
